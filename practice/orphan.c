#include <stdio.h>
#include <stdlib.h>
#include <sys/types.h>
#include <unistd.h>

void main()
{

    pid_t pid, childpid, mypid, parentpid;
    if (pid < -1)
    {
        printf("The child process couldnt be created\n");
    }

    if (pid == 0)
    {
        printf("")
    }
    else
    {
    }
}